# Database (Prisma)
DATABASE_URL="postgres://..."

# JWT
JWT_SECRET="change-in-production"

# ── SiteGround SMTP (pentru trimitere cod verificare) ──
# Obține aceste valori din Site Tools → Email → Accounts
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Baterino <noreply@tudomeniu.com>"
SITE_NAME="Baterino Romania"

# Base URL for frontend (e.g. https://baterino.ro) – used in password reset emails
FRONTEND_URL="http://localhost:5173"

# ── Cloudflare R2 (imagini și documente produse) ──
R2_ENDPOINT="https://YOUR_ACCOUNT_ID.eu.r2.cloudflarestorage.com"
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET="baterinoromania"
R2_PUBLIC_URL="https://media.baterino.ro"
