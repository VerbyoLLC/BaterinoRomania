# Optional: set to "instalatori-only" to show only Instalatori page + partner login (no register).
# Used e.g. on Vercel staging. Leave unset for full site.
# VITE_SITE_MODE=instalatori-only

# Optional: API base URL (production/staging). Dev uses proxy /api or localhost.
# VITE_API_URL=https://your-api.vercel.app
